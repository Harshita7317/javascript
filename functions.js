//addition
const add =( a,b) => {
    let c=a+b;
    return c;
};
 let sol1=add(5,6);
 console.log(sol1);
 
 //subtraction
 const sub =( d,e) => {
    let f=d-e;
    return f;
};
 let sol2=sub(11,6);
 console.log(sol2);
 
//multiplication
 const mul=(x,y) => {
    let m=x*y;
    return m;
 };
 let multiply=mul(10,3);
 console.log(multiply);

 //divsion
 const div=(u,v) => {
    let k=u/v;
    return k;
 };
 let division=div(10,2);
 console.log(division);

